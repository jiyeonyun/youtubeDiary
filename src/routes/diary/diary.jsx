import React from 'react';
import Header from '../../components/header/header';

const Diary = (props) => {
    return(
        <>
        <Header/>
        diary
        </>
    );
};

export default Diary;